// src/users/entity/user-role.enum.ts

/**
 * Enum for defining user roles within the application.
 * Used for role-based access control (RBAC).
 */
export enum UserRoleEnum {
  USER = 'user',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}
